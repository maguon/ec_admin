import {createAction} from 'redux-actions';

export const setMenuList = createAction('SET_MENU_LIST');
export const setConditionUserType = createAction('SET_CONDITION_USER_TYPE');
export const setCurrentUserType = createAction('SET_CURRENT_USER_TYPE');